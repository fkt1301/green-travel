name: green_travel
version: '1.0.0'
profile: green_travel

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  green_travel:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
